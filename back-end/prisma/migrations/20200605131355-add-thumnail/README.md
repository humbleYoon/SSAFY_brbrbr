# Migration `20200605131355-add-thumnail`

This migration has been generated by Bae,Seong-ho at 6/5/2020, 1:13:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "broom"."Event" ADD COLUMN "thumburl" text  NOT NULL ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200601162454-add-robot-name..20200605131355-add-thumnail
--- datamodel.dml
+++ datamodel.dml
@@ -2,40 +2,42 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Place {
-  id           Int      @default(autoincrement()) @id
-  name         String
-  description  String
-  floor        Int
-  xaxis        Float   
-  yaxis        Float
-  thumburl     String
-  mapurl       String
+  id          Int    @default(autoincrement()) @id
+  name        String
+  description String
+  floor       Int
+  xaxis       Float
+  yaxis       Float
+  thumburl    String
+  mapurl      String
   @@unique([name, floor])
+  Event Event[]
 }
 model Event {
-  id           Int      @default(autoincrement()) @id
-  name         String   @unique
-  description  String
-  starttime    DateTime
-  endtime      DateTime
-  place        Place    @relation(fields: [placeName, placeFloor], references: [name, floor])
-  placeName    String
-  placeFloor   Int      
+  id          Int      @default(autoincrement()) @id
+  name        String   @unique
+  description String
+  starttime   DateTime
+  endtime     DateTime
+  place       Place    @relation(fields: [placeName, placeFloor], references: [name, floor])
+  placeName   String
+  placeFloor  Int
+  thumburl    String
 }
 model Robot {
-  id           Int      @default(autoincrement()) @id
-  name         String   @unique
-  floor        Int      
-}
+  id    Int    @default(autoincrement()) @id
+  name  String @unique
+  floor Int
+}
```


