# Migration `20200531160749-init`

This migration has been generated by Bae,Seong-ho at 5/31/2020, 4:07:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "broom"."Place" (
"description" text  NOT NULL ,"floor" integer  NOT NULL ,"id" SERIAL,"mapurl" text  NOT NULL ,"name" text  NOT NULL ,"thumburl" text  NOT NULL ,"xaxis" Decimal(65,30)  NOT NULL ,"yaxis" Decimal(65,30)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "broom"."Event" (
"description" text  NOT NULL ,"endtime" timestamp(3)  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,"placeId" integer  NOT NULL ,"starttime" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "broom"."Event" ADD FOREIGN KEY ("placeId")REFERENCES "broom"."Place"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200531160749-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Place {
+  id           Int      @default(autoincrement()) @id
+  name         String
+  description  String
+  floor        Int
+  xaxis        Float   
+  yaxis        Float
+  thumburl     String
+  mapurl       String
+}
+
+model Event {
+  id           Int      @default(autoincrement()) @id
+  name         String
+  description  String
+  starttime    DateTime
+  endtime      DateTime
+  place        Place    @relation(fields: [placeId], references: [id])
+  placeId      Int      
+}
```


