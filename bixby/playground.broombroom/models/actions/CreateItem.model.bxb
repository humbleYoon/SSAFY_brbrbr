action (CreateItem) {
  type (Constructor)
  collect {
    input (placeAndEvent) {
      type (PlaceAndEvent)
      min (Required)

      default-init {
        intent {
          goal: FindPlaceAndEvent
        }
      }
    }
  }
  output (Item)
}