action (FindPlaceAndEvent) {
  type (Search)
  collect {
    input (placeName) {
      type (PlaceName)
      min (Optional)
    }
    input (eventName) {
      type (EventName)
      min (Optional)
    }
  }
  output (PlaceAndEvent)
}
