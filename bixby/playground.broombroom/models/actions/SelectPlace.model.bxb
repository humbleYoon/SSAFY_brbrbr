action (SelectPlace) {
  type(Constructor)
  description (특정 장소에 대해 이동)
  collect {
    input (selectedPlaceName) {
      type (SelectedPlaceName)
      min (Optional) max (One)
    }
  }
  output (Destination)
}
