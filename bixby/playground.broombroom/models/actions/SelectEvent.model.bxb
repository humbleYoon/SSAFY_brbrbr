action (SelectEvent) {
  type(Constructor)
  description (특정 행사에 대해 이동)
  collect {
    input (selectedEventName) {
      type (SelectedEventName)
      min (Optional) max (One)
    }
  }
  output (SelectedEventName)
}
