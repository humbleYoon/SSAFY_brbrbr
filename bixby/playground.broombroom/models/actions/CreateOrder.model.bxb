action (CreateOrder) {
  type (BeginTransaction)
  collect {
    input (authenticationState) {
      type (AuthenticationState)
      min (Optional)
      max (One)
    }
    input (item) {
      type (Item)
      min (Optional)
      max (One)
    }
  }
  output (Order)
}