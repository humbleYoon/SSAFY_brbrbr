structure (CheckedAuthenticationCode) {
  description (백엔드로부터 인증받은 정보)
  property (authenticationCode) {
    type (AuthenticationCode)
    min (Required)
    max (One)
  }
  property (isAuthenticated) {
    type (IsAuthenticated)
    min (Required)
    max (One)
  }
}
