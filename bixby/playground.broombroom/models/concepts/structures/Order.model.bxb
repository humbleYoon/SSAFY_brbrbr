structure (Order) {
  description (오더)
  property (item) {
    type(Item)
    min (Required)
    max (One)
  }
  
  property (orderNumber) {
    type (OrderNumber)
    min (Required)
  }
  features {
    transaction
  }
}
