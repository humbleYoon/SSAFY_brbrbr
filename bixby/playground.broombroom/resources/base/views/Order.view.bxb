result-view {
  match: Order (this)

  render {
    layout {
      section {
        content {
          single-line {
            text ("${value(this.item)}")
          }
          single-line {
            text ("${value(this.orderNumber)}")
          }
        }
      }
    }
  }
}